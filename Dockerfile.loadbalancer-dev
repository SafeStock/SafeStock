FROM nginx:1.27-alpine

# Copiar configuração do load balancer para dev (hot reload)
COPY nginx-loadbalancer-dev.conf /etc/nginx/conf.d/default.conf

# Expor porta
EXPOSE 80

# Comando para iniciar nginx
CMD ["nginx", "-g", "daemon off;"]
