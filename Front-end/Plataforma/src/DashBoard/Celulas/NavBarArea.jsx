import { NavBar } from "../Moleculas/NavBar.jsx";

export function NavBarArea() {

    return (
    <div className="flex items-center justify-center w-[11vw] h-[100vh] overflow-hidden relative z-20">
        <NavBar/>
        </div>
    );
}