# Configuração para produção (AWS)
# Este arquivo é usado automaticamente pelo Vite em modo production (npm run build)

# Em produção, usa o proxy reverso do Nginx (mesmo domínio)
VITE_API_BASE_URL=/api

# Timeout menor em produção
VITE_API_TIMEOUT=10000
