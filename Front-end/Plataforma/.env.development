# Configuração para desenvolvimento local
# Este arquivo é usado automaticamente pelo Vite em modo development
# Funciona tanto no Codespace quanto na máquina local com Docker

# Backend local (Docker na mesma máquina)
VITE_API_BASE_URL=http://localhost:8081/api

# Timeout para requisições (30 segundos)
VITE_API_TIMEOUT=30000
